name: flutter_ddd_architecture
description: Build clean architecture app with domain driven design

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  freezed_annotation: ^1.1.0
  json_annotation: ^4.4.0
  dartz: ^0.10.1
  internet_connection_checker: ^0.0.1+3
  shared_preferences: ^2.0.13
  injectable: ^1.5.3
  get_it: ^7.2.0
  http: ^0.13.4
  cupertino_icons: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  build_runner: ^2.1.8
  freezed: ^1.1.1
  json_serializable: ^6.1.5
  injectable_generator: ^1.5.3

flutter:
  uses-material-design: true
